type: concat

sources:
  - type: dataset
    spec: dataset/ufreiburg-flyingchairs2.yaml

    parameters:
      type: test
      direction: forwards

  - type: dataset
    spec: dataset/ufreiburg-flyingchairs2.yaml

    parameters:
      type: test
      direction: backwards
