type: concat

sources:
  - type: dataset
    spec: dataset/ufreiburg-driving.yaml

    parameters:
      type: train
      pass: final
      direction: forwards
      camera: left

  - type: dataset
    spec: dataset/ufreiburg-driving.yaml

    parameters:
      type: train
      pass: final
      direction: forwards
      camera: right

  - type: dataset
    spec: dataset/ufreiburg-driving.yaml

    parameters:
      type: train
      pass: final
      direction: backwards
      camera: left

  - type: dataset
    spec: dataset/ufreiburg-driving.yaml

    parameters:
      type: train
      pass: final
      direction: backwards
      camera: right
