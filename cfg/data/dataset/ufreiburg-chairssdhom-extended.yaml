name: Uni Freiburg ChairsSDHom (extended)
id: ufreiburg-chairssdhom-extended
path: '../../../../datasets/ufreiburg-chairssdhom-extended/data'

layout:
  type: generic

  images: '{type}/{seq:07d}-img_{idx:d}.png'
  flows: '{type}/{seq:07d}-flow_01.flo'
  key: '{type}/{seq:07d}'

parameters:
  type:
    values: [ train, test ]

    sub:
      test: { type: val }
      train: { type: train }
