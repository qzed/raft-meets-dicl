type: concat

sources:
  - type: dataset
    spec: dataset/ufreiburg-flyingthings3d.yaml

    parameters:
      type: test
      pass: final
      direction: forwards
      camera: left

  - type: dataset
    spec: dataset/ufreiburg-flyingthings3d.yaml

    parameters:
      type: test
      pass: final
      direction: forwards
      camera: right

  - type: dataset
    spec: dataset/ufreiburg-flyingthings3d.yaml

    parameters:
      type: test
      pass: final
      direction: backwards
      camera: left

  - type: dataset
    spec: dataset/ufreiburg-flyingthings3d.yaml

    parameters:
      type: test
      pass: final
      direction: backwards
      camera: right
