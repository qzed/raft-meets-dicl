type: concat

sources:
  - type: dataset
    spec: dataset/ufreiburg-monkaa.yaml

    parameters:
      pass: clean
      direction: forwards
      camera: left

  - type: dataset
    spec: dataset/ufreiburg-monkaa.yaml

    parameters:
      pass: clean
      direction: forwards
      camera: right

  - type: dataset
    spec: dataset/ufreiburg-monkaa.yaml

    parameters:
      pass: clean
      direction: backwards
      camera: left

  - type: dataset
    spec: dataset/ufreiburg-monkaa.yaml

    parameters:
      pass: clean
      direction: backwards
      camera: right
