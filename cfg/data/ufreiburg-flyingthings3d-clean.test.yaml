type: concat

sources:
  - type: dataset
    spec: dataset/ufreiburg-flyingthings3d.yaml

    parameters:
      type: test
      pass: clean
      direction: forwards
      camera: left

  - type: dataset
    spec: dataset/ufreiburg-flyingthings3d.yaml

    parameters:
      type: test
      pass: clean
      direction: forwards
      camera: right

  - type: dataset
    spec: dataset/ufreiburg-flyingthings3d.yaml

    parameters:
      type: test
      pass: clean
      direction: backwards
      camera: left

  - type: dataset
    spec: dataset/ufreiburg-flyingthings3d.yaml

    parameters:
      type: test
      pass: clean
      direction: backwards
      camera: right
