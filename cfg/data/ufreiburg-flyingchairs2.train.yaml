sources:
  - type: dataset
    spec: dataset/ufreiburg-flyingchairs2.yaml

    parameters:
      type: train
      direction: forwards

  - type: dataset
    spec: dataset/ufreiburg-flyingchairs2.yaml

    parameters:
      type: train
      direction: forwards
